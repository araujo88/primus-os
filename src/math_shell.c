#include "../include/math_shell.h"
#include "../include/string.h"

#include "stdint.h"

uint32_t parse_int(char *string)
{
    uint32_t i;
    char *parser;
    while (string[i] != '\0')
    {
    }
}